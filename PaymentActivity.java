package com.example.madcourseproject;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class PaymentActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.payment_interface);

        // You can perform any additional operations on the views here if needed
        // For example, you can find the ImageView by its ID and set properties or listeners
        // ImageView imageView = findViewById(R.id.imageView);
        // imageView.setImageResource(R.drawable.pay);
    }
}

